enum TmsPath {
  LINE = "/tms",
  TEACHER = "/teacher",
  WORK = "/work",
  LEAVE = "/leave",
}

enum TmsFullPath {
  TEACHER = "/tms/teacher",
  WORK = "/tms/work",
  LEAVE = "/tms/leave",
}

export { TmsPath, TmsFullPath };
